# MedSAM2 inference service — base HTTP deps.
fastapi>=0.110
uvicorn[standard]>=0.27
numpy>=1.24
pydantic>=2.0

# PyTorch and SAM2/MedSAM2 are intentionally NOT pinned here because they are
# platform/GPU specific and large. Install them separately, e.g.:
#
#   pip install torch torchvision            # pick the right build for your CUDA/MPS/CPU
#   pip install "git+https://github.com/facebookresearch/sam2.git"
#
# Then download a MedSAM2 checkpoint and set the environment variables
# MEDSAM2_CHECKPOINT and MEDSAM2_CONFIG (see README.md).
